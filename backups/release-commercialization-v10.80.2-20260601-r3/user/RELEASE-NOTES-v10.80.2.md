# v10.80.2 修复记录（2026-05-28）

## 已确认修复
1. 省心模式在“主题强调图 / 未声明图片”下，已恢复正常出图（不再出现普遍无图）。
2. 针对白色简约等浅色极简诉求，新增 themeAccent 稳定性兜底：自动回退网图源，减少空白占位图。
3. 管理员入口统一支持 `/admin`，移除前端路径硬限制导致的不可访问问题。

## 反馈模块
1. 结果页评分交互改为“星星评分 + 拖动条”，替代下拉框。
2. 若 `ppt_feedback` 表尚未迁移，反馈将自动降级写入 `credit_transactions(type=feedback)`，后台可继续统计展示，不阻塞使用。

## 备注
- 保留数据库迁移文件：`supabase-migrations/02-ppt-feedback.sql`。
- 建议后续补执行该迁移后，反馈将自动优先写入 `ppt_feedback` 正式表。
