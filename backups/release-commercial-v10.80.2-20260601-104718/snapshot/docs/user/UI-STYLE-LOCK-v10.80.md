# 省心PPT UI 锁版记录（v10.80）

日期：2026-05-25  
环境：Production  
线上域名：https://shengxinppt.lol  
部署ID：dpl_5uFZWTYfmaN2gUkzLUZRyVAANWUT

## 锁定范围

本次锁定针对首页高级选项中的「主题色系」模块（推荐色系后面的正式分类）：

1. 模块文案统一为「主题色系」，不再使用「主题风格」
2. 推荐色系保持原逻辑、原排序、原内容
3. 正式色系按固定映射分组（不再使用自动猜测）
4. 色系展示顺序固定：蓝色系、橙棕色系、米黄色系、绿色系、白灰色系、黑色系、紫粉色系
5. 每个主题只能归属一个色系，不重复出现
6. 每个色系最多 10 个主题卡（每行最多 5 个，最多 2 行）
7. 临时命名「橙红色·2」停止使用，统一为语义化命名（如米黄色系）

## 核心备注（业务确认）

后续我会实际的调用，以验证是否生成结果对齐选择的色系。  
参数和主题是否真实对应生成ppt出来的结果，需验证。

## 锁定文件

- `src/lib/theme-database.ts`
- `src/components/ThemeSelector.tsx`
- `src/components/ThemePickerModal.tsx`
- `src/lib/version.ts`
- `package.json`

## 锁定说明

后续仅允许做数据准确性与功能性修复，不允许在未确认前改动该模块的分类体系、排序逻辑、命名规范和展示结构。  
如需变更，需新增一条“解锁记录”并注明原因。
