# v10.95.26 - M3 多模态与 Gamma 可视化大纲

发布日期：2026-07-06

## 变更

- MiniMax 默认模型切换为 `MiniMax-M3`，文本备用为 `MiniMax-M2.7`。
- 图片理解优先走 `MiniMax-M3`，失败后回退 `MiniMax-VL-01`。
- 大纲新增 `visualType`、`layoutIntent`、`chartSpec`、`diagramSpec`、`iconHints`、`imageIntent` 字段。
- Gamma Markdown 增加逐页 `Gamma可视化约束`，强化流程、时间线、矩阵、图表、数字看板和图标卡片映射。
- Gamma 附加提示新增原生可视化映射规则，避免退化为纯文字页。
- 同步更新 MiniMax 架构文档与诊断接口模型。

## 验证

- `npx tsc --noEmit`
- `npm test -- --run`
- `npm run build`
- 本地网页 review：桌面端和 390x844 移动端首屏无控制台错误，无横向溢出。
- 使用 `PPT说明书 中文版.docx` 前 5 页抽取文本做本地大纲/Markdown 验收。

## 发布备注

- 本地环境未配置 `MINIMAX_API_KEY`，真实 M3 + Gamma 生成链路由线上环境继续审核。
- 发布前已创建本地备份目录与 Git 版本标记。
