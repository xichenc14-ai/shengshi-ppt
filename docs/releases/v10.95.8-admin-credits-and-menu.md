# v10.95.8 管理员积分与菜单修复

- 手机菜单删除重复的「定价方案」入口。
- 登录用户菜单顺序改为：用户中心、升级套餐、功能特性。
- 首页功能锚点绑定到实际的场景与流程模块，点击「功能特性」会准确滚动到对应区域。
- 管理员菜单、用户中心和后台管理统一显示数据库中的真实用户积分，不再用 Gamma Key 池估算值覆盖。
- Session 读取时从数据库同步最新积分、套餐和用户资料，生成扣分后刷新页面不会恢复旧数值。
- 后台积分调整增加余额条件校验，防止并发覆盖；扣分不能产生负余额。
- 后台积分调整返回数据库实际余额，并记录积分流水状态。
